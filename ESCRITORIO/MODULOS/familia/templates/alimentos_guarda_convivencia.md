# TEMPLATE DE MINUTA — Alimentos c/c Guarda e Convivência (esqueleto — Fase 4)

> Template validado no markup que a skill `formatacao-peticoes-nascimento`
> aceita (`#`, `##`, `-`, `>`). Todo parágrafo fático/de pedido carrega tag de
> rastreabilidade em linha própria:
>
> `<!-- SOJ: F04 | P03,P05 | PED01 | CC:1694 -->`
>
> (ordem dos campos: fatos | provas | pedidos | fundamentos — separados por `|`;
>  vários IDs no mesmo campo separados por vírgula; campo vazio = `-`)
>
> O gate G3 lê as tags para auditar a cobertura; o `preparar_protocolo.py` as
> remove antes da formatação. **O template real será extraído da minuta_v01 do
> CASO_TESTE_001 na Fase 4** — até lá, minutas de família partem do zero dentro
> do kernel (seção 9 do blueprint: o primeiro caso constrói o módulo).

(vazio de propósito — Fase 4)
